[user]
	email = bradley@vektor.nyc
	name = Bradley Cicenas
[core]
	editor = vim
	pager = cat
[push]
	default = simple
[alias]
	pullall = "!f(){ git pull \"$@\" && git submodule update --init --recursive; }; f"
	statusall = "submodule status --recursive"
	diffall = "!f(){ git diff \"$@\"; }; f"
